# 部署检查清单

## ✅ 已完成的步骤

- [x] Git 仓库已初始化
- [x] Git 用户信息已配置（hailiandeng07-prog / hailiandeng07@gmail.com）
- [x] 所有文件已添加到 Git（40个文件，4545行代码）
- [x] 初始提交已创建
- [x] 远程仓库地址已配置（https://github.com/hailiandeng07-prog/shuntai-website.git）

## ⏳ 需要完成的操作

### 1. 推送到 GitHub

推送时遇到网络超时，请尝试：

**方式A：重新推送**
```bash
git push -u origin main
```

**方式B：如果推送失败，检查网络连接**
```bash
# 测试 GitHub 连接
ping github.com

# 如果网络正常，重试推送
git push -u origin main
```

**方式C：使用 SSH（如果配置了 SSH key）**
```bash
# 更改远程仓库地址为 SSH
git remote set-url origin git@github.com:hailiandeng07-prog/shuntai-website.git

# 推送
git push -u origin main
```

**注意：** 如果提示需要认证，GitHub 现在使用 Personal Access Token 而不是密码。

### 2. 创建 GitHub Personal Access Token（如果需要）

如果推送时提示需要认证：

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 填写名称，勾选 `repo` 权限
4. 点击 "Generate token"
5. 复制 token（只显示一次）
6. 推送时使用 token 作为密码

### 3. 构建生产版本

```bash
# 确保在项目目录
cd "/Users/hailian-test/Desktop/cursor 1.0/建站_260120 0.3"

# 安装依赖（如果还没安装）
npm install

# 构建生产版本
npm run build
```

构建完成后，`dist/` 目录包含所有需要部署的文件。

### 4. 部署到阿里云服务器

详细步骤请参考 `备份部署步骤.md` 文件。

**快速部署步骤：**

1. **上传文件到服务器**
   ```bash
   # 使用 rsync（推荐）
   rsync -avz --delete dist/ root@your-server-ip:/var/www/html/
   
   # 或使用 SCP
   scp -r dist/* root@your-server-ip:/var/www/html/
   ```

2. **配置 Nginx**
   - SSH 登录服务器
   - 编辑 Nginx 配置文件
   - 确保包含 SPA 路由支持：`try_files $uri $uri/ /index.html;`

3. **重启 Nginx**
   ```bash
   sudo systemctl restart nginx
   ```

## 📊 项目统计

- **总文件数**: 40 个文件
- **代码行数**: 4,545 行
- **图片资源**: 7 张（已本地化）
- **页面数量**: 9 个页面

## 🔗 相关链接

- **GitHub 仓库**: https://github.com/hailiandeng07-prog/shuntai-website
- **部署指南**: 查看 `备份部署步骤.md`
- **问题排查**: 查看 `TROUBLESHOOTING.md`

## 下一步

1. 完成 GitHub 推送（重试上面的推送命令）
2. 构建生产版本（`npm run build`）
3. 准备服务器环境
4. 上传并配置网站

如有问题，随时告诉我！
